AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://<your-resource-name>.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# ── Email (SMTP): "Email me the summary" feature ─────────────────────────────
# SMTP = how apps send email. You only need: your email + an app password.
#
# Gmail (easiest):
#   1. Use your Gmail address as SMTP_USER (e.g. you@gmail.com).
#   2. Do NOT use your normal Gmail password. Create an "App Password":
#      → Go to https://myaccount.google.com/apppasswords
#      → Sign in, select "Mail" and "Other (Custom name)" → e.g. "Sales AI"
#      → Copy the 16-character password and paste as SMTP_PASSWORD (no spaces).
#   3. Keep SMTP_HOST and SMTP_PORT as below.
#
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-char-app-password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
#
# Other providers (uncomment and fill if not using Gmail):
# Outlook / Microsoft:  SMTP_HOST=smtp.office365.com   SMTP_PORT=587
# Yahoo:                SMTP_HOST=smtp.mail.yahoo.com SMTP_PORT=587
# (Use the email and app password for that account.)
